# Frontend Partition

The React frontend source is currently in `src/`.

This folder marks the frontend partition in the project layout.
If you want, I can move all frontend files under `frontend/` in a follow-up refactor.

